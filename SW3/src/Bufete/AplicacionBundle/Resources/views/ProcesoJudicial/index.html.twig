{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProcesoJudicial list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Estado</th>
                <th>Codigoconsulta</th>
                <th>Fechain</th>
                <th>Fechavencimiento</th>
                <th>Prioridad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('procesojudicial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.estado }}</td>
                <td>{{ entity.codigoConsulta }}</td>
                <td>{% if entity.fechaIn %}{{ entity.fechaIn|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaVencimiento %}{{ entity.fechaVencimiento|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.prioridad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('procesojudicial_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('procesojudicial_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('procesojudicial_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
